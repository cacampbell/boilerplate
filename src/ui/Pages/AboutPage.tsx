import { JSX, h } from "preact";
import React from "react";

export default (): JSX.Element => {
    return (
        <div>
            <h2>About</h2>
            <p>This is about.</p>
        </div>
    );
};
