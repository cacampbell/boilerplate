import { JSX, h } from "preact";
import React from "react";

export default (): JSX.Element => {
    return (
        <div>
            <h2>Not Found</h2>
            <p>Sorry this page was not found. Go back to Home</p>
        </div>
    );
};
